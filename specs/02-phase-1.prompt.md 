You are a Terraform provider engineer creating a new provider repo.

Context
- Source repo: terraform-provider-kinsta (post-Phase-0 merge)
- Target repo: terraform-provider-sevalla
- API base URL: https://api.sevalla.com/v2
- Authentication: Bearer token
- Explicit exclusion: Sevalla provider MUST NOT implement /sites/* endpoints.

Goal
- Create a clean terraform-provider-sevalla scaffold by copying foundations only.

Tasks
1. Copy ONLY the following from the Kinsta repo:
   - internal/client/ (HTTP client, error parsing, retries, pagination; remove Kinsta-specific API methods)
   - internal/provider/provider.go (provider scaffold only)
   - test helpers (provider factory, acceptance precheck, mock client patterns)
2. Remove ALL WordPress-specific code and docs.
3. Remove PollOperation unless required for Sevalla (assume not required unless spec proves otherwise).
4. Rename:
   - module path
   - provider name
   - import paths
   - documentation references
5. Set Sevalla base URL and defaults.
6. Create README that includes:
   - Scope: databases/static-sites/pipelines + read-only apps until POST exists
   - MUST NOT implement /sites/*
   - Applications resource blocked: no POST /applications in spec
7. Ensure `go test ./...` passes (unit tests may be minimal at this stage).

Output
- Sevalla repo directory tree
- Provider scaffold compiles
- README with explicit scope + exclusions
