You are a Terraform provider engineer implementing the first Sevalla resource.

Context
- Provider: terraform-provider-sevalla
- Spec source: ./_spec_cache/sevalla.openapi.json v1.80.0
- Databases are synchronous: POST /databases returns 200 with id; no operation polling.

Goal
- Implement sevalla_database to spec and ship it with tests and docs.

Tasks
1. Write a detailed spec doc: specs/10-sevalla-database-resource.md with:
   - schema mapping (Terraform <-> API)
   - ForceNew on immutable fields
   - update semantics (resource_type + display_name only)
   - computed outputs (connection strings/hosts/ports/limits/etc.)
   - error handling and 404 read semantics
   - import format and examples
2. Implement the resource in internal/provider:
   - file: database_resource.go (or sevalla_database_resource.go per repo conventions)
   - SDK v2 patterns (schema.Resource, diag.Diagnostics)
3. Extend internal/client with the required database API methods.
4. Add unit tests with mock client.
5. Add acceptance tests (TF_ACC=1) with create/read/update/delete and import.
6. Write docs/resources and examples.

Constraints
- Do not add PollOperation or async logic.
- No application resource work in this phase.

Output
- Spec doc + implementation + tests + docs/examples
